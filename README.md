# cc-opus-swarm

A simple way to allow multiple Claude Code instances to collaborate on projects.

Customize roles, number of instances spawned, etc.

Nothing crazy, uses terminals, requires nothing but Python.
